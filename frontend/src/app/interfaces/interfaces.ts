export interface DeviceModel {
    id: number;
    categoryId: number;
    categoryName: string;
    color: string;
    partNumber: number;
  }

  export interface CategoryModel {
      id: number;
      name: string;
  }